// Diamond DS icon sizes (see the Icons foundation page: Storybook > Foundations
// > Icons). There's no `LucideIcon` wrapper yet - the docs say to import icons
// straight from lucide-react and size them by hand until one ships - so these
// are the `size`/`strokeWidth` props to spread onto each icon.
export const ICON_XS = { size: 16, strokeWidth: 1.5 };
export const ICON_SM = { size: 20, strokeWidth: 1.75 };
export const ICON_MD = { size: 24, strokeWidth: 2 };
