apiVersion: v1
kind: Service
metadata:
  name: {{include "xrddatavis.fullname" .}}
  labels: {{- include "xrddatavis.labels" . | nindent 4}}
spec:
  type: {{.Values.service.type}}
  ports:
    - port: {{.Values.service.port}}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "xrddatavis.selectorLabels" . | nindent 4}}
